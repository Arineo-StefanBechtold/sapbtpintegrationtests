# Test Data — Task 01 Collector

This folder contains sample requests and expected outputs for collector contract tests.

Suggested structure:

- `request/` — raw sample requests
- `expected/` — expected manifests, headers, and payloads
- `examples/` — small API examples for docs and tests
